"use client";

import React from "react";

export default function Page() {
  return (
    <main className="wrap">
      <div className="top">
        <div className="kicker">YOUR CHOICES · PROJECT COMPLETE</div>
      </div>

      <div className="center">
        <h1 className="title">
          Builders<span className="accent">DISPATCH</span>
        </h1>

        <p className="sub">
          A modern platform for the construction industry — connecting people, projects, and execution.
        </p>

        <div className="ctaRow">
          <button className="btn" onClick={() => (window.location.href = "/next-test")}>
            DISPATCH NOW
          </button>
        </div>
      </div>

      <div className="footer">© 2026 BuildersDISPATCH · All rights reserved</div>

      <style jsx>{`
        :global(html),
        :global(body) {
          margin: 0;
          padding: 0;
        }

        .wrap {
          --accent: #ff5f15;

          min-height: 100vh;
          display: grid;
          grid-template-rows: auto 1fr auto;
          padding: 28px 18px;
          color: #fff;

          background:
            radial-gradient(900px 600px at 25% 20%, rgba(90, 140, 255, 0.30), transparent 60%),
            radial-gradient(900px 600px at 80% 70%, rgba(60, 120, 220, 0.22), transparent 60%),
            radial-gradient(700px 500px at 55% 35%, rgba(255, 95, 21, 0.12), transparent 62%),
            linear-gradient(180deg, #0b0f16 0%, #0a1222 45%, #07101b 100%);
        }

        .top {
          display: flex;
          justify-content: center;
          align-items: center;
          padding-top: 6px;
        }

        .kicker {
          text-align: center;
          font-size: 16px;
          font-weight: 800;
          letter-spacing: 0.14em;
          text-transform: uppercase;
          color: rgba(255, 255, 255, 0.82);
        }

        .center {
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          text-align: center;
          max-width: 980px;
          margin: 0 auto;
          padding: 12px 0;
        }

        .title {
          margin: 0 0 14px 0;
          font-size: 64px;
          line-height: 1.02;

          /* modern bold, closer to button vibe */
          font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji",
            "Segoe UI Emoji";
          font-weight: 900;
          letter-spacing: 0.01em;
        }

        .accent {
          color: var(--accent);
          text-shadow: 0 0 18px rgba(255, 95, 21, 0.18);
        }

        .sub {
          margin: 0 0 28px 0;
          font-size: 20px;
          line-height: 1.55;
          color: rgba(255, 255, 255, 0.84);
          max-width: 820px;
        }

        .ctaRow {
          display: flex;
          justify-content: center;
          margin: 4px 0 0 0;
        }

        .btn {
          padding: 14px 22px;
          border-radius: 12px;
          border: 1px solid rgba(0, 0, 0, 0.35);
          background: var(--accent);
          color: #07101b;
          font-weight: 900;
          font-size: 16px;
          cursor: pointer;
          box-shadow: 0 14px 34px rgba(255, 95, 21, 0.14);
          transition: transform 120ms ease, filter 120ms ease;
        }

        .btn:hover {
          transform: translateY(-1px);
          filter: brightness(1.04);
        }

        .btn:active {
          transform: translateY(0px);
          filter: brightness(0.98);
        }

        .footer {
          display: flex;
          justify-content: center;
          align-items: center;
          padding-bottom: 6px;
          font-size: 13px;
          color: rgba(255, 255, 255, 0.6);
          text-align: center;
        }

        @media (max-width: 640px) {
          .kicker {
            font-size: 13px;
            letter-spacing: 0.12em;
          }
          .title {
            font-size: 44px;
          }
          .sub {
            font-size: 17px;
          }
        }
      `}</style>
    </main>
  );
}
